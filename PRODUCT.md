# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**주 사용자 — 납품받은 클라이언트의 사이트 운영자 (비개발자).**
중소기업·기관의 담당 직원으로, 개발자가 떠난 뒤 홈페이지를 혼자 굴린다. 공지 올리기, 페이지 문구 수정, 배너·팝업 교체, 문의 확인 및 답장이 일상 업무이며, 이 일을 하려고 개발자를 다시 부르는 순간 제품이 실패한 것이다. `docs/manual.md` 와 `docs/AICreo-매뉴얼.pdf` 가 이 사용자를 위해 존재한다.

부차 사용자:

- **1인 웹에이전시 개발자(제작자)** — 클라이언트마다 저장소를 복제해 초기 세팅·테마 제작·배포를 수행한다. 목표는 단순 홈페이지 3~5일, 중형 사이트 1~2주 납품.
- **다른 개발자** — 저장소가 GitHub 공개(`pushwing/AICreo`) + MIT 라이선스라 외부에서 clone 해 쓸 수 있다. README·샘플 테마·매뉴얼이 사실상 제품 표면의 일부다. 다만 이들을 위해 운영자 경험을 희생하지 않는다.
- **최종 방문자** — 납품된 사이트의 고객. 프론트 테마·SEO 품질이 이들에게 닿는다.

## Product Purpose

클라이언트 홈페이지를 반복 제작할 때 **코어는 재사용하고 껍데기만 교체**하는 CodeIgniter 4 보일러플레이트다. 게시판 CMS·동적 페이지·문의·회원·마케팅 오버레이·SEO 를 이미 갖춘 상태에서 시작해, 클라이언트 고유성은 테마와 관리자 설정으로만 흡수한다.

성공의 정의는 두 겹이다:

1. **납품 속도** — 저장소 복제부터 배포까지 단순 홈페이지 3~5일.
2. **운영 자립** — 납품 후 클라이언트 운영자가 개발자 호출 없이 사이트를 운영한다. 운영자가 못 해서 개발자를 부르는 요청이 곧 제품 결함 리포트다.

## Positioning

특정 서비스가 아니라 **재판매되는 템플릿**이다(`docs/seo-geo-strategy.md` §0). 그래서 어떤 결정도 특정 도메인·업종을 가정하지 않고 **설정 주입**으로 동작해야 한다는 제약이 1순위다.

이웃 제품이 그대로 베끼기 어려운 지점:

- **테마 교체가 운영자 손에 있다.** ZIP 업로드 → 검증(필수 파일·Zip-slip 차단·확장자 화이트리스트·`default` 예약어 보호) → 설치 → 카드에서 클릭 한 번 전환. `ThemeView` 가 활성 테마 → `default` 테마 → 원본 경로 순으로 해석해, 새 테마는 **바꾸고 싶은 파일만** 두면 나머지는 자동 폴백된다.
- **SEO 가 아니라 GEO 까지 보일러플레이트에 내장.** `sitemap.xml`·`robots.txt`·`llms.txt`·IndexNow 자동 색인 제출이 제품 기능이라, 이 위에 배포되는 모든 클라이언트 사이트가 자동으로 혜택받는다. AI 검색 크롤러(GPTBot·PerplexityBot·ClaudeBot 등) 정책도 관리자 스위치로 노출하는 설계.
- **콘텐츠 뷰와 테마의 분리.** 게시판·인증·관리자 뷰는 테마 영향을 받지 않아, 테마 제작자가 CMS 동작을 깨뜨릴 수 없다.

## Operating Context

- **납품 워크플로우** — 복제 → `.env` + `php spark migrate` → `/admin` 사이트 설정 → 메뉴·페이지 작성 → `public/themes/{클라이언트}/` + `app/Views/themes/{클라이언트}/` 커스텀 → 테마 전환 → 배포.
- **운영자의 일상 도구는 `/admin` 단 하나** — 대시보드, 페이지, 게시판, 전체 게시물, 배너, 팝업, 회원, 메뉴, 미디어 라이브러리, 문의 수신함, 사이트 설정(기본·연락처·SNS·SEO·푸터), 테마, 소셜 로그인, 스케줄. 미읽음 문의는 전 관리자 화면에 알림으로 뜬다.
- **새 클라이언트 적용 시 바뀌는 것**은 전부 관리자 화면 안에 있다 — 회사명·연락처·주소·SNS·GA/네이버 인증·메뉴·페이지 내용·게시판 권한·로고/파비콘. 코드에서 바꾸는 것은 테마뿐이다.
- **운영 서버는 cron 1줄**(`php spark tasks:run`)로 돌고, 활성화·주기는 `/admin/schedule` 에서 운영자가 관리한다.
- 서버 시각은 UTC, 앱·화면은 `Asia/Seoul`. 배너·팝업 노출 기간 판정이 여기에 걸려 있다.

## Capabilities and Constraints

**확정된 기능** — 동적 페이지(슬러그 라우팅·레이아웃 선택), 다중 게시판(게시판별 비회원/회원/관리자 읽기·쓰기 권한, TinyMCE 6, 첨부 최대 10MB 확장자 화이트리스트, 비회원 작성·수정, 댓글, 비밀글, 공지 상단 고정 최대 5개, 검색), 문의폼(DB 저장 + 메일 발송), 회원 인증 및 Google·Naver·Kakao 소셜 로그인, DB 기반 2단계 GNB, 위치별 배너·기간/좌표 기반 팝업, 미디어 라이브러리, SEO/GEO 엔드포인트.

**기술 제약**

- CodeIgniter 4.7 · MySQL/MariaDB · Bootstrap 5 · TinyMCE 6(CDN, API 키 `.env`) · CI4 세션 인증 · CI4 파일 캐시.
- 테마는 `views/layouts/main.php` 와 `public/css/style.css` 를 필수로 요구하고, 테마명 `default` 는 예약어다.
- 게시판 검색이 `LIKE '%키워드%'` 구조라 인덱스를 타지 못한다. 수만 건 이상 쌓이는 사이트에서는 FULLTEXT 전환이 필요하다.
- 시각 컬럼은 `TIMESTAMP` + 커넥션 세션 타임존 규약을 따른다.

**PHP 런타임 — `^8.5` 유지 (2026-08-14 결정)**

`composer.json` 의 `"php": "^8.5"` 를 그대로 둔다. 납품 대상에 국내 공유호스팅(카페24·가비아 등)이 포함되고 이들 다수가 PHP 8.1~8.3 에 머물러 있다는 사실은 그대로지만, **실제 그 환경에 납품할 일이 생기는 시점에 다시 판단**하기로 했다. 지금은 모던 문법과 성능을 제약 없이 쓴다.

그때 선택지는 세 갈래로 남아 있다 — 하한을 낮추거나, 공유호스팅을 대상에서 빼거나, 환경별로 분기하거나. 다만 **하한을 낮추는 선택은 8.5 전용 문법이 쌓일수록 비싸진다**는 점만 기억해 두면 된다. 지금 이 사실을 이유로 코드를 보수적으로 쓸 필요는 없다.

**미결 항목 없음.** 남아 있던 두 가지(PHP 런타임 하한, KWCAG 준수 등급)는 모두 2026-08-14 에 결정됐다.

## Brand Commitments

- 제품명 표기는 **`AICreo`** 로 통일한다(2026-08-14 결정). `AiCreo` 는 쓰지 않는다 — 저장소명(`pushwing/AICreo`)·README·러너 설정이 이미 이 표기다.
- 라이선스 **MIT**, 저장소 공개.
- 문서·UI 문구는 한국어가 기본이다.
- 제품 자체는 고유 비주얼 아이덴티티를 주장하지 않는다 — 정체성은 각 클라이언트의 테마에 있고, 코어는 그 그릇으로 중립을 유지한다.

## Evidence on Hand

- `README.md` — 기능·설치·납품 워크플로우·테마 패키징 규격.
- `docs/manual.md` · `docs/AICreo-매뉴얼.pdf` — 운영자·개발자 통합 매뉴얼. 관리자 화면별 입력 필드·옵션·동작 수록.
- `docs/seo-geo-strategy.md` — SEO → GEO 전략, 크롤링 표면 진단, 멀티테넌트 설정 주입 원칙.
- `roadmap.md` — 배너·팝업·성능 개선의 구현 파일 목록과 트러블슈팅 기록.
- 샘플 테마 3종 `dark.zip` · `violet.zip` · `spring.zip` — 테마 시스템이 레이아웃 수준까지 교체 가능함을 실증(`spring` 은 좌측 배너 사이드바로 레이아웃 자체가 다름).
- 기본 관리자 계정 `admin@example.com` / `admin1234!` (마이그레이션 시딩, 최초 로그인 후 변경 전제).

**없는 것 — 지어내면 안 된다.** 실제 납품 클라이언트 사례, 고객 후기, 트래픽·전환 수치, 가격·라이선스 판매 조건, 성능 벤치마크는 저장소에 존재하지 않는다.

## Product Principles

1. **운영자가 개발자 없이 끝낸다.** 사이트별로 달라지는 것은 전부 관리자 화면의 설정 주입으로 가능해야 한다. 운영자가 하려던 일에 코드 수정이 필요하다면 그것은 요청이 아니라 결함이다.
2. **코어는 재사용, 껍데기만 교체.** 클라이언트 고유성은 테마와 설정에 격리한다. 한 클라이언트를 위한 변경이 코어에 스며들면 다음 납품이 느려진다.
3. **업종·도메인을 가정하지 않는다.** 재판매되는 템플릿이므로 모든 기본값은 중립이고, 구체성은 클라이언트가 채운다.
4. **접근성은 기본값이지 옵션이 아니다.** KWCAG 준수가 계약 조건이므로, 나중에 덧붙이는 감사 항목이 아니라 기본 컴포넌트가 처음부터 만족한 상태여야 한다.
5. **납품 속도가 성과 지표다.** 새 기능이 납품 단계를 늘린다면 그 기능은 관리자 설정으로 흡수되거나 빠져야 한다.

## Accessibility & Inclusion

**KWCAG(한국형 웹 콘텐츠 접근성 지침) 2.2 준수가 필수다.** 공공·공공성 기관 납품이 있어 계약 조건에 해당하며, 앞으로의 모든 작업이 이를 깨뜨리지 못하는 제약으로 취급한다.

실무적으로 이는 코어와 `default` 테마가 이미 만족해야 함을 뜻한다 — 대체 텍스트, 키보드만으로 도달·조작 가능한 내비게이션과 관리자 화면, 명도 대비, 폼 레이블과 오류 안내의 프로그래밍적 연결, 초점 표시, 팝업·배너 같은 오버레이의 닫기 수단과 초점 관리. 테마가 이를 되돌릴 수 있으므로 테마 제작 규격에도 이 기준이 걸린다.

**준수 등급은 AA 로 확정한다 (2026-08-14 결정).** A 가 아니라 AA 이므로 아래 기준이 추가로 계약 범위에 들어온다 — 새 화면을 만들 때 이것들이 자동으로 딸려온다는 뜻이다.

- **명도 대비 4.5:1**(큰 텍스트·비텍스트 3:1). 코어 토큰은 이미 이 선을 넘도록 실측해 두었다 — `DESIGN.md` 의 «The Measured Contrast Rule».
- **입력 목적 식별**(1.3.5) — 이름·이메일·연락처 입력에 `autocomplete` 를 붙인다.
- **리플로**(1.4.10) — 320px 폭에서 가로 스크롤 없이 읽혀야 한다.
- **초점 표시**(2.4.7)와 **초점 가림 금지**(2.4.11, 2.2 신설) — 초점 링이 보이는 것만으로는 부족하고, sticky 상단바 같은 요소에 가려서도 안 된다.
- **타깃 크기 24×24**(2.5.8, 2.2 신설).
- **오류 안내와 정정 제안**(3.3.3) — 무엇이 왜 틀렸는지 텍스트로 말한다.
- **상태 메시지**(4.1.3) — 초점을 옮기지 않고 바뀐 내용을 알린다(`role="alert"` · `aria-live`).

인증 심사 대상 사이트의 범위는 계약 건별로 정한다 — 코어는 어느 사이트가 심사를 받든 통과할 수 있는 상태를 유지한다.

**아직 실측하지 않은 것** — 320px 폭 리플로(1.4.10)는 390px 까지만 확인했다. 스크린리더 실사용 검증(NVDA·VoiceOver)도 하지 않았다. 자동 검사로는 잡히지 않는 영역이므로, 실제 인증 심사를 앞두면 이 두 가지를 먼저 확인한다.
